# Copy to .env and set your own values. Never commit .env.

# Postgres (used by docker-compose and DATABASE_URL)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=set_a_strong_password
POSTGRES_DB=shows

# App connection string — must match user/password/db above
DATABASE_URL="postgresql://postgres:set_a_strong_password@localhost:5432/shows"

# Homebrew/Postgres.app: postgresql://YOUR_MAC_USERNAME@localhost:5432/shows
# Production: set DATABASE_URL from Neon/Supabase in your host (e.g. Vercel env).
