{
  "name": "personal-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run covers:fetch && next dev",
    "build": "prisma generate && next build",
    "start": "next start",
    "lint": "eslint",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:seed": "prisma db seed",
    "db:studio": "prisma studio",
    "covers:fetch": "tsx scripts/fetch-show-covers.ts",
    "episodes:fetch": "tsx scripts/fetch-episodes.ts",
    "ranks:backfill": "tsx scripts/backfill-absolute-ranks.ts",
    "data:sync": "pnpm run db:push && pnpm run db:seed && pnpm run ranks:backfill && pnpm run covers:fetch && pnpm run episodes:fetch",
    "prod:db": "node scripts/check-database-url.mjs && pnpm run db:push && pnpm run db:seed && pnpm run ranks:backfill && pnpm run covers:fetch && pnpm run episodes:fetch"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "prisma": "^5.22.0",
    "tailwindcss": "^4",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}